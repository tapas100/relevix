apiVersion: 1

providers:
  - name: Relevix
    orgId: 1
    folder: Relevix
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
